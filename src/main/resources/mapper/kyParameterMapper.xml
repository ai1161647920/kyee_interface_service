<?xml version="1.0" encoding="UTF-8"?>
<!-- 
    Welcome to use the TableGo Tools.
    
    http://www.tablego.cn
    
    http://vipbooks.iteye.com
    http://blog.csdn.net/vipbooks
    http://www.cnblogs.com/vipbooks
    
    Author: bianj
    Email: tablego@qq.com
    Version: 6.6.6
-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 方法参数(ky_parameter) -->
<mapper namespace="com.kyee.iszx.mapper.KyParameterMapper">
    <!-- 字段映射 -->
    <resultMap id="kyParameterMap" type="com.kyee.iszx.bean.KyParameter"></resultMap>
    
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        kyParameter.id, kyParameter.name_cn, kyParameter.name_en, kyParameter.mark, kyParameter.description, kyParameter.attribute, kyParameter.affectContent, kyParameter.method_id, 
        kyParameter.type, kyParameter.other_claim, kyParameter.value_item, kyParameter.value
    </sql>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findKyParameterAll" resultMap="kyParameterMap">
        SELECT
        <include refid="allColumns" />
        FROM ky_parameter kyParameter
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findKyParameterByCondition" resultMap="kyParameterMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM ky_parameter kyParameter WHERE 1 = 1
        <if test="nameCn != null and nameCn != ''">
            AND kyParameter.name_cn LIKE CONCAT('%', #{nameCn}, '%')
        </if>
        <if test="nameEn != null and nameEn != ''">
            AND kyParameter.name_en LIKE CONCAT('%', #{nameEn}, '%')
        </if>
        <if test="mark != null">
            AND kyParameter.mark = #{mark}
        </if>
        <if test="description != null and description != ''">
            AND kyParameter.description LIKE CONCAT('%', #{description}, '%')
        </if>
        <if test="attribute != null">
            AND kyParameter.attribute = #{attribute}
        </if>
        <if test="affectContent != null and affectContent != ''">
            AND kyParameter.affectContent LIKE CONCAT('%', #{affectContent}, '%')
        </if>
        <if test="methodId != null">
            AND kyParameter.method_id = #{methodId}
        </if>
        <if test="type != null">
            AND kyParameter.type = #{type}
        </if>
        <if test="otherClaim != null and otherClaim != ''">
            AND kyParameter.other_claim LIKE CONCAT('%', #{otherClaim}, '%')
        </if>
        <if test="valueItem != null">
            AND kyParameter.value_item = #{valueItem}
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findKyParameterByIds" resultMap="kyParameterMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM ky_parameter kyParameter WHERE kyParameter.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertKyParameter" parameterType="map">
        INSERT INTO ky_parameter (
            id, name_cn, name_en, mark, description, attribute, affectContent, method_id, 
            type, other_claim, value_item
        ) VALUES (
            #{id},
            #{nameCn},
            #{nameEn},
            #{mark},
            #{description},
            #{attribute},
            #{affectContent},
            #{methodId},
            #{type},
            #{otherClaim},
            #{valueItem},
            #{value}
        )
    </insert>
    
    <!-- 批量插入数据 -->
    <insert id="insertKyParameters" parameterType="list">
        INSERT INTO ky_parameter (
            id, name_cn, name_en, mark, description, attribute, affectContent, method_id, 
            type, other_claim, value_item
        ) VALUES
        <foreach collection="list" index="index" item="item" separator=",">
            (
                #{item.id},
                #{item.nameCn},
                #{item.nameEn},
                #{item.mark},
                #{item.description},
                #{item.attribute},
                #{item.affectContent},
                #{item.methodId},
                #{item.type},
                #{item.otherClaim},
                #{item.valueItem},
                #{item.value}
            )
        </foreach>
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateKyParameter" parameterType="map">
        UPDATE ky_parameter
        <set>
            <if test="nameCn != null">
                name_cn = #{nameCn},
            </if>
            <if test="nameEn != null">
                name_en = #{nameEn},
            </if>
            <if test="mark != null">
                mark = #{mark},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="attribute != null">
                attribute = #{attribute},
            </if>
            <if test="affectContent != null">
                affectContent = #{affectContent},
            </if>
            <if test="methodId != null">
                method_id = #{methodId},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="otherClaim != null">
                other_claim = #{otherClaim},
            </if>
            <if test="valueItem != null">
                value_item = #{valueItem}
            </if>
            <if test="value != null">
                value_item = #{value}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateKyParameters" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE ky_parameter
            <set>
                <if test="item.nameCn != null">
                    name_cn = #{item.nameCn},
                </if>
                <if test="item.nameEn != null">
                    name_en = #{item.nameEn},
                </if>
                <if test="item.mark != null">
                    mark = #{item.mark},
                </if>
                <if test="item.description != null">
                    description = #{item.description},
                </if>
                <if test="item.attribute != null">
                    attribute = #{item.attribute},
                </if>
                <if test="item.affectContent != null">
                    affectContent = #{item.affectContent},
                </if>
                <if test="item.methodId != null">
                    method_id = #{item.methodId},
                </if>
                <if test="item.type != null">
                    type = #{item.type},
                </if>
                <if test="item.otherClaim != null">
                    other_claim = #{item.otherClaim},
                </if>
                <if test="item.valueItem != null">
                    value_item = #{item.valueItem}
                </if>
                <if test="item.value != null">
                    value = #{item.value}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateKyParametersValue" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE ky_parameter
            <set>
                value = #{item.value}
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteKyParameterById" parameterType="int">
        DELETE FROM ky_parameter WHERE id = #{id}
    </delete>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteKyParameterByIds" parameterType="list">
        DELETE FROM ky_parameter WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>